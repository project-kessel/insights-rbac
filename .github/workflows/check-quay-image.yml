name: Check Quay Image

on:
  check_suite:
    types:
      - completed

jobs:
  check-image:
    runs-on: ubuntu-latest
    if: >-
      github.event.check_suite.head_branch == 'master' &&
      github.event.check_suite.conclusion == 'success' &&
      github.event.check_suite.app.slug == 'red-hat-konflux'
    steps:
      - name: Wait for Quay image
        uses: petracihalova/check-quay-image@v1
        with:
          quay_repo: redhat-services-prod/hcc-accessmanagement-tenant/insights-rbac
          commit_sha: ${{ github.event.check_suite.head_sha }}
          slack_webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
          delay_minutes: 5
